/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { ComalXpressApp } from './ComalXpressApp';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => ComalXpressApp);
